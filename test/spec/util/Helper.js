describe('Helper', function () {
  it('should expose a public API');

  it('can extend objects');
  it('can mixin  objects into prototypes');
  it('can clone  objects');
  it('can check for css classes on dom elements');
  it('can check for css classes on dom elements');
  it('can add css classes on dom elements');
  it('can remove css classes from dom elements');
  it('can add css style strings to the document');
});
